rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // OPEN ACCESS FOR EASY SYSTEM (No Firebase Auth required)
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
